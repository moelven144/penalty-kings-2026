{
  "name": "penalty-world-cup-2026",
  "version": "1.1.0",
  "private": true,
  "description": "Penalty Kings 2026 — penalty-shootout tournament game (web + iOS via Capacitor)",
  "scripts": {
    "serve": "python3 -m http.server 8000 --directory www",
    "sync": "cap sync",
    "ios": "cap open ios",
    "icons": "capacitor-assets generate --ios"
  },
  "dependencies": {
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.4.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
